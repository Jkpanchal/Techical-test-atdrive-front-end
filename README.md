# CrudAppFe

Tech Stack: Angular, Material UI

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.3.5.

## Development server

check if angular cli is installed or not if it is not installed then do install from npm i -g "@angular/cli" after run npm install to install project dependecy and run below command to work with weather API provide API inside weatherServcie.ts file in API_KEY property 

to Generate API

go to https://www.weatherapi.com/ sign up and create free tier api key and to test API key

follow  the URL like this  https://api.weatherapi.com/v1/current.json?key=API_KEY&q=Noida

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.
